import { PropsWithChildren } from 'react';

/**
 * Layout wrapper for the contribute section
 */
export default function ContributeLayout({ children }: PropsWithChildren) {
  return <main>{children}</main>;
}
